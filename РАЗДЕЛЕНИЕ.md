# Разделение по пользователям (продажа доступа другим) — аудит и план

Дата аудита: 19.09.2026. Отмечайте пункты по мере выполнения.

## Что уже сделано

- Браузер не подключается к базе напрямую: все запросы идут через серверные функции, пароль базы в браузер не попадает.
- Список доступных баз считается на сервере по вошедшему пользователю; подменить «свою базу» из браузера в обычных списках нельзя.
- Разрешён только заранее известный список таблиц — произвольную таблицу запросить нельзя.
- Роли: владелец, менеджер, кладовщик, наблюдатель. Наблюдатель не пишет; настройки базы, сотрудники и организации — только владелец.
- Приглашения сотрудников по email с токеном и сроком 14 дней.
- Журнал изменений и журнал просмотров важных разделов.
- Вход: пароли scrypt, сессия в зашифрованной cookie, ограничение 8 неудачных попыток за 15 минут.
- Админка платформы закрыта признаком администратора; произвольный SQL — только чтение, максимум 200 строк.

## Что надо добавить (по важности)

### Блокеры

1. [x] Новый пользователь не может создать первую базу: при создании базы проверка прав требует наличие уже существующей базы. Исправить: разрешить создание собственной базы владельцем.
2. [x] Чтение записи по её номеру (getById) не проверяет базу — чужую запись можно прочитать, зная id. Добавить проверку доступа.
3. [x] Сохранение «по совпадению ключа» (upsert) ищет запись по всей базе данных, без ограничения своей базой — можно перезаписать чужую запись. Ограничить поиск своими базами.
4. [x] Картинки и файлы: отдаются без входа по угадываемому адресу, не привязаны к базе, любой вошедший может перезаписать или удалить чужой файл. Добавить привязку к базе, проверку доступа при загрузке/удалении и при выдаче.
5. [x] История документа (docHistory) отдаётся без проверки доступа к базе. Добавить проверку.

### Важное

6. [x] Поток изменений в реальном времени рассылает события всех клиентов всем — фильтровать по доступным базам.
7. [x] Вложенные связи в запросах (товар у строки документа и т. п.) подтягиваются без проверки базы — ограничить доступными базами.
8. [x] Записи без привязки к базе видны всем (сейчас 6 папок товаров) — раздать привязку и запретить создание таких записей.
9. [ ] Развести «администратор платформы» и «владелец базы»; убедиться, что клиент никогда не видит раздел админки.
10. [ ] Удаление пользователя: чистить участие в базах, приглашения и журналы.

### Для продажи (тарифы и учёт)

11. [x] Признак «аккаунт активен / доступ приостановлен» и блокировка входа при неоплате.
12. [x] Тариф и лимиты: число баз, сотрудников, товаров, документов, места под файлы; понятное сообщение при достижении лимита.
13. [x] Регистрация: подтверждение email или приглашение/код, чтобы не заводили аккаунты произвольно.
14. [x] Карточка клиента в админке: тариф, срок оплаты, объём данных, последняя активность; возможность приостановить и продлить.
15. [x] Резервные копии и удаление данных по клиенту (выдать выгрузку, удалить по требованию).

## Сделано 20.09.2026 (этап 1 — изоляция клиентов)

- Сохранение «по совпадению кода» ищет запись только в текущей базе и не может увести чужую запись (проверка базы и в самом обновлении).
- Дополнение записи по совпадению номера (id) тоже проверяет доступ к базе.
- Открытие записи по номеру — только внутри доступных баз.
- Записи без базы больше не общие: список общих справочников пуст, запись без базы запрещена (кроме создания своей первой базы).
- Новый пользователь может создать свою первую базу; владельцем становится он сам.
- Связанные данные (позиции, товар, контрагент, справочники) подтягиваются с проверкой базы.
- Имена полей проверяются отдельной функцией, условия выборки берутся в скобки.
- В рабочей базе 6 подпапок цветов без привязки привязаны к базе «Еще одна»; других «бесхозных» записей нет.
- Файл настроек исключён из репозитория, добавлен .env.example, адрес старой базы убран из скрипта переноса.
- Проверено на рабочих данных: чужой документ не читается и не перехватывается, импорт по коду 1С обновляет запись без дублей, документы/позиции/контрагенты/касса/оплаты/справочники открываются как раньше.

Осталось (этап 2): файлы и картинки по базам, история документа и поток изменений с проверкой доступа, числовые сравнения сумм, проверка входных данных схемой, строгий сертификат подключения к базе.

## Сделано 20.09.2026 (этап 2 — файлы, история, поток изменений, проверки)

- Картинки товаров привязаны к базе: загрузка и удаление — только своей базы и только с правом на запись, размер до 5 МБ, разрешены только картинки.
- Просмотр картинки требует входа и участия в той же базе; адрес больше не публичный.
- История документа отдаётся только по своим базам.
- Поток изменений в реальном времени отдаёт события только своих баз.
- Суммы и количества сравниваются как числа (раньше «9» считалось больше «10» — фильтры «от суммы» давали лишние документы).
- Все данные, приходящие из браузера в серверные функции, проверяются по схеме (таблица, имена полей, роли, e-mail, размеры).
- Соединение с базой: если база на этом же сервере — без шифрования; если на другом — шифрование, а при наличии корневого сертификата (PGSSLROOTCERT) ещё и строгая проверка.

## Сделано 21.09.2026 (этап 3 — независимость от платформы)

- Убрана библиотека прежнего хранилища (`@supabase/supabase-js`) — она больше не использовалась.
- Удалён служебный файл отправки ошибок в Lovable; ошибки пишутся в обычный журнал браузера.
- Папка `supabase/` удалена, её старые миграции сохранены в `sql/archive-supabase/` как история.
- Письма (сброс пароля, приглашения) уходят напрямую через Resend по вашему ключу `RESEND_API_KEY` — без посредника Lovable. Отправитель настраивается переменной `MAIL_FROM`.
- Из `.env` убраны старые публичные адреса Supabase.
- README переписан: это описание вашей CRM, запуск, сборка, деплой и список документов.
- Добавлен самостоятельный конфиг сборки `vite.config.standalone.ts` — проект собирается без пакетов платформы.

### Что сделать вручную при полном уходе с платформы

1. Переименовать `vite.config.standalone.ts` в `vite.config.ts` (прежний сохранить как `vite.config.lovable.ts`), затем `bun remove @lovable.dev/vite-tanstack-config` и проверить сборку.
   Пока правки делаются в Lovable, оставить текущий конфиг — иначе редактор и предпросмотр перестанут работать.
2. В GitHub убрать право записи у `lovable-dev[bot]`.

## Этап 4 — выполнено (21.09.2026)

- [x] Версионированные миграции: папка `sql/migrations`, запуск `node scripts/migrate.mjs`
      (`--dry` — показать, что будет применено), учёт применённых в таблице `schema_migrations`.
      Базовая схема для чистой установки — `db/schema.sql`.
- [x] Пароль при регистрации, смене и восстановлении — не короче 8 символов
      (в том числе при создании пользователя из админки).
- [x] Ограничение регистраций: не более 5 новых аккаунтов в час с одного адреса.
- [x] Раздельные счётчики блокировки входа: 5 неудачных попыток на один e-mail
      и 15 с одного адреса за 15 минут.
