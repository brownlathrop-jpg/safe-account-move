rules_version = '2';
// Доступ к данным CRM: только авторизованным пользователям.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
